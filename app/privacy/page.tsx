import { LegalPlaceholder } from "@/components/legal-placeholder";

export const metadata = { title: "Privacy · BountyDesk" };

export default function PrivacyPage() {
  return (
    <LegalPlaceholder title="Privacy policy">
      The policy is not published yet. What the app stores today is the reports it is sent,
      the evidence from reproducing them, and who approved which verdict.
    </LegalPlaceholder>
  );
}
