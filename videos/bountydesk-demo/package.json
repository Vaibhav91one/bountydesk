{
  "name": "bountydesk-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build:components": "esbuild src/recording-components.jsx --bundle --format=iife --minify --outfile=assets/vendor/recording-components.min.js",
    "dev": "npm run build:components && npx --yes hyperframes@0.8.20 preview",
    "check": "npm run build:components && npx --yes hyperframes@0.8.20 check",
    "render": "npm run build:components && npx --yes hyperframes@0.8.20 render --quality high --output renders/bountydesk-demo-hyperframes.mp4",
    "publish": "npx --yes hyperframes@0.8.20 publish"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
